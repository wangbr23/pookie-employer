# T7 brief — Add Alembic migration framework only

Task:
- [ ] `T7` Add Alembic migration framework only — agent, depends-on: T6, design: docs/designs/2026-08-31-pookie-employer.md
  - Done when: backend has Alembic installed/configured, migration commands documented, Alembic imports backend settings/database metadata, and an empty/no-op migration workflow can run without defining domain tables yet.

Relevant context:
- Backend is Python 3.12+ FastAPI under `backend/`.
- Backend owns database schema/migrations.
- DB stack: PostgreSQL, SQLAlchemy 2.x, Alembic.
- T6 already added `pookie_backend.config.Settings`, `get_settings()`, `pookie_backend.database.Base`, `engine`, `SessionLocal`, and `create_db_engine()`.
- Do not add domain tables in this task; T36/T37 do that later.
- Keep this limited to Alembic framework/config, dependency/docs/commands, and tests/checks proving the no-op migration workflow works.

Expected likely changes:
- Add Alembic dependency to backend dependency files/pyproject.
- Add `backend/alembic.ini` and `backend/alembic/env.py` (plus versions dir) configured to import backend settings and `Base.metadata`.
- Add Makefile commands for migration workflows, e.g. current/check/create/upgrade/downgrade as appropriate.
- Document migration commands in `backend/README.md`.
- Add test or check that Alembic config imports metadata/settings and can inspect current heads without requiring domain tables.

Rules:
- No product behavior.
- No domain schema tables or migrations beyond no-op/bootstrap if needed.
- No secrets. Use env/config already established.
- Run backend checks: `cd backend && make test && make lint && make typecheck`.
